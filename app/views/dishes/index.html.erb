<div>
  <h1>Pratos</h1>
</div>

<%= link_to 'Cadastrar novo Prato', new_dish_path %>
<br>
<table>
  <tr>
    <th>delete</th>
    <th>edit</th>
    <th>Restaurante</th>
    <th>Prato</th>
    <th>Preço</th>
  </tr>
  <% @dishes.each do |dish| %>
    <tr>
      <td><%= link_to 'Excluir', dish_path(dish.id), method: :delete %></td>
      <td><%= link_to 'Editar', edit_dish_path(dish.id) %></td>
      <td><%= dish.restaurante.name %></td>
      <td><%= dish.name %></td>
      <td><%= dish.price %></td>
    </tr>
  <% end %>
</table>
